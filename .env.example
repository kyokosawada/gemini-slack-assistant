# Slack — new app in the "Development" workspace (Socket Mode).
# Bot User OAuth Token (Install App page). Starts with xoxb-.
SLACK_BOT_TOKEN=
# App-Level Token, scope connections:write (Basic Information → App-Level Tokens). Starts with xapp-.
SLACK_APP_TOKEN=
